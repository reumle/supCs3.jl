## This file autogenerated by BinaryProvider.write_deps_file().
## Do not edit.
##
## Include this file within your main top-level source, and call
## `check_deps()` from within your module's `__init__()` method

if isdefined((@static VERSION < v"0.7.0-DEV.484" ? current_module() : @__MODULE__), :Compat)
    import Compat.Libdl
elseif VERSION >= v"0.7.0-DEV.3382"
    import Libdl
end
const indirect = joinpath(dirname(@__FILE__), "../../supJ/out/libscsindir.so")
const direct = joinpath(dirname(@__FILE__), "../../supJ/out/libscsdir.so")
function check_deps()
    global indirect
    if !isfile(indirect)
        error("$(indirect) does not exist, Please re-run Pkg.build(\"supCs3\"), and restart Julia.")
    end

    if Libdl.dlopen_e(indirect) in (C_NULL, nothing)
        error("$(indirect) cannot be opened, Please re-run Pkg.build(\"supCs3\"), and restart Julia.")
    end

    global direct
    if !isfile(direct)
        error("$(direct) does not exist, Please re-run Pkg.build(\"supCs3\"), and restart Julia.")
    end

    if Libdl.dlopen_e(direct) in (C_NULL, nothing)
        error("$(direct) cannot be opened, Please re-run Pkg.build(\"supCs3\"), and restart Julia.")
    end

end
